[include]
    path = ~/.gitconfig.local

[core]
    excludesfile = ~/.gitignore

[color]
    ui = auto

[push]
    default = current

[alias]
    br = branch
    co = checkout
    cm = commit --message
